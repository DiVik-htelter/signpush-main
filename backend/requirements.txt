# ============================================================================
# Backend Dependencies для SignPush - Система подписи документов
# ============================================================================

# FastAPI - современный асинхронный веб-фреймворк
fastapi==0.104.1

# Uvicorn - ASGI сервер для запуска FastAPI приложения
uvicorn[standard]==0.24.0

# Pydantic - валидация данных и настройки
pydantic==2.5.0

# PostgreSQL драйвер для Python
psycopg2-binary==2.9.9

# CORS middleware (уже включен в FastAPI, но указываем для ясности)
python-multipart==0.0.6

# httpx - асинхронный HTTP клиент для внешних запросов
httpx==0.25.0

# ============================================================================
# PDF обработка и манипуляция
# ============================================================================

# PyMuPDF (fitz) - библиотека для работы с PDF документами
# Позволяет встраивать изображения подписей в PDF
PyMuPDF==1.23.8

# Pillow - обработка изображений (используется PyMuPDF)
Pillow==10.1.0

# ============================================================================
# Дополнительные утилиты
# ============================================================================

# python-dotenv - загрузка переменных окружения из .env файла
python-dotenv==1.0.0

# ============================================================================
# Опционально: для криптографической цифровой подписи (ЭЦП)
# ============================================================================

# cryptography - криптографические операции
# Раскомментируйте, если нужна ЭЦП
# cryptography==41.0.7

# pyhanko - библиотека для PDF подписей с сертификатами
# Раскомментируйте для полноценной ЭЦП
# pyhanko-certvalidator==0.26.2

# ============================================================================
# Опционально: для интеграции со скважинными данными и внешними API
# ============================================================================

# Redis - кэширование и сессии (если понадобятся)
# redis==5.0.1

# ============================================================================
# ИНСТРУКЦИЯ ПО УСТАНОВКЕ:
# ============================================================================
# 
# 1. Создайте виртуальное окружение (если еще не создано):
#    python -m venv venv
#
# 2. Активируйте виртуальное окружение:
#    Windows: venv\Scripts\activate
#    Linux/Mac: source venv/bin/activate
#
# 3. Установите зависимости:
#    pip install -r requirements.txt
#
# 4. Запустите сервер:
#    python main.py
#    или
#    uvicorn main:app --reload
# ============================================================================
